This project is a simple weather app built with vanilla JavaScript, HTML, and CSS. It calls a public weather API to fetch current conditions and render them on the page. The goal is to practice core API skills in JS—making requests, handling JSON responses, basic error handling, and updating the DOM—laying the groundwork for future API-driven web apps and eventually building APIs of my own
